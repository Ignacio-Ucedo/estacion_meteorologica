## 1. Infraestructura (ChirpStack)

- [x] 1.1 Corregir las 8 frecuencias de canal de 125 kHz y el canal de 500 kHz en `infra/chirpstack/region_au915_2.toml` de la fórmula US915 (`902.3 + n×0.2`) a la fórmula AU915 (`915.2 + n×0.2`): 903.9→916.8, 904.1→917.0, 904.3→917.2, 904.5→917.4, 904.7→917.6, 904.9→917.8, 905.1→918.0, 905.3→918.2 MHz (canales 8–15), y el canal 500 kHz 904.6→917.5 MHz. Actualizar también los comentarios del archivo (cabecera, `description`, fórmula documentada). No requiere hardware físico. Commit sugerido: `fix(infra): corregir canales AU915 sub-band 2 de fórmula US915 a AU915 en ChirpStack`.
- [x] 1.2 Corregir comentarios con "902–928 MHz" / "903.9 MHz" en `infra/chirpstack/chirpstack.toml` y `infra/docker-compose.yml` para reflejar 915–928 MHz / 916.8 MHz. No requiere hardware físico. Commit sugerido: `docs(infra): corregir comentarios de banda AU915 en chirpstack.toml y docker-compose.yml`.

## 2. Firmware del gateway

- [x] 2.1 Corregir la constante `AU915_SUBBAND2_FREQ_HZ` en `firmware/lr1121-transceiver/src/lib.rs` de `903_900_000` a `916_800_000` y su comentario ("903.9 MHz" → "916.8 MHz"). No requiere hardware físico para compilar. Commit sugerido: `fix(gateway): corregir AU915_SUBBAND2_FREQ_HZ de 903.9 MHz a 916.8 MHz en lr1121-transceiver`.
- [x] 2.2 Corregir logs y comentarios en `firmware/src/bin/gateway-node.rs` ("Canal fijo: 903.9 MHz...", `info!("channel=903.9MHz...")`, `info!("...903.9 MHz SF7BW125")`) para reflejar 916.8 MHz. No requiere hardware físico para compilar; validación en banco queda cubierta por la tarea 7.1/7.2 de `migrate-lr1121-au915` cuando haya hardware. Commit sugerido: `fix(gateway): actualizar logs de gateway-node a 916.8 MHz AU915 sub-band 2`.

## 3. Firmware del nodo sensor

- [x] 3.1 Corregir el comentario de rango de canales en `firmware/lr1121-modem-e/src/lib.rs` ("903.9–905.3 MHz" → "916.8–918.2 MHz"). Sin cambio funcional esperado: el Modem-E resuelve la frecuencia física internamente a partir de `Region::Au915` + máscara de canal por índice, no de un valor de frecuencia manual. No requiere hardware físico. Commit sugerido: `docs(firmware): corregir comentario de rango de canales AU915 sub-band 2 en lr1121-modem-e`.

## 4. Documentación del proyecto

- [x] 4.1 Corregir `openspec/config.yaml`: "AU915 (902–928 MHz)" → "AU915 (915–928 MHz)"; "903.9 MHz SF7BW125 (canal 8, sub-band 2)" → "916.8 MHz SF7BW125 (canal 8, sub-band 2)" (aparece en la sección del nodo sensor y en la del gateway). No requiere hardware físico. Commit sugerido: `docs(docs): corregir banda y canal AU915 en openspec/config.yaml`.
- [x] 4.2 Corregir `CLAUDE.md`: "Canal fijo PoC: 903.9 MHz SF7BW125" (diagrama de arquitectura), "Listens on 903.9 MHz SF7BW125", y "operating in the AU915 band (902–928 MHz)" → valores corregidos (916.8 MHz, banda 915–928 MHz). No requiere hardware físico. Commit sugerido: `docs(docs): corregir banda y canal AU915 en CLAUDE.md`.
- [x] 4.3 Corregir `hardware/netlist.md`: "RX 903.9 MHz SF7BW125" en la tabla de componentes del gateway → "RX 916.8 MHz SF7BW125". No requiere hardware físico. Commit sugerido: `docs(hardware): corregir frecuencia de canal AU915 en netlist.md`.

## 5. Corrección de la change hermana `migrate-lr1121-au915`

- [x] 5.1 Corregir `openspec/changes/migrate-lr1121-au915/design.md`: valores "903.9 MHz" (D3 y flujo de datos extremo a extremo) y el rango "902.3–914.9 MHz" citado como AU915 en D3 (reemplazar por el rango AU915 real 915.2–927.8 MHz uplink 125 kHz + 915.9–927.1 MHz uplink 500 kHz). No requiere hardware físico. Commit sugerido: `docs(docs): corregir frecuencias AU915 en design.md de migrate-lr1121-au915`.
- [x] 5.2 Corregir `openspec/changes/migrate-lr1121-au915/proposal.md`: "902–928 MHz AU915" → "915–928 MHz AU915 (regulado por ENACOM)". No requiere hardware físico. Commit sugerido: `docs(docs): corregir rango de banda AU915 en proposal.md de migrate-lr1121-au915`.
- [x] 5.3 Corregir `openspec/changes/migrate-lr1121-au915/tasks.md`: referencias literales a "903.9 MHz" y "903_900_000 Hz" en las tareas 4.3 y 8.1 → 916.8 MHz / 916_800_000 Hz. No requiere hardware físico. Commit sugerido: `docs(docs): corregir frecuencia AU915 citada en tasks.md de migrate-lr1121-au915`.
- [x] 5.4 Corregir las tres delta specs de `migrate-lr1121-au915` (`specs/lorawan-gateway-au915/spec.md`, `specs/lorawan-node-au915/spec.md`, `specs/lr1121-driver/spec.md`): todas las referencias a "903.9 MHz" y "903_900_000" → 916.8 MHz / 916_800_000. No requiere hardware físico. Commit sugerido: `docs(docs): corregir frecuencia AU915 en delta specs de migrate-lr1121-au915`.

## 6. Verificación

- [x] 6.1 Correr una búsqueda de texto sobre todo el repo (`903\.9|904\.[1679]|905\.[13]|902\.3|902–928|902-928`) y confirmar cero resultados fuera de contexto histórico/archivado (p. ej. `openspec/changes/archive/` de la migración SX1276, que documenta una arquitectura ya reemplazada y no se corrige). No requiere hardware físico. Commit sugerido: `chore(docs): verificar ausencia de referencias residuales a 903.9 MHz US915`.
  - Resultado: únicamente quedan matches intencionales (notas de este propio change y de `migrate-lr1121-au915` que documentan el valor incorrecto anterior como referencia) y matches en `openspec/changes/archive/` (histórico, sin corregir). Se detectó además `AGENTS.md`, que describe toda la arquitectura pre-migración (EU433/SX1278) — es staleness preexistente no relacionada con este bug puntual de fórmula de frecuencia; queda fuera de alcance de este change.
- [x] 6.2 Verificar que `firmware/lr1121-transceiver` y `firmware/src/bin/gateway-node.rs` compilan tras el cambio de constante (`cargo build` con el target ESP32 configurado, o al menos `cargo check` si el toolchain xtensa no está disponible en este entorno). No requiere hardware físico para compilar. Commit sugerido: `chore(firmware): verificar compilación tras corrección de frecuencia AU915`.
  - Resultado (actualizado): se instaló el toolchain `esp` (espup 0.17.1 + Xtensa Rust 1.95.0.0) en este entorno y se corrió `cargo check --bin gateway-node`. Se detectaron y resolvieron dos problemas ajenos a este fix: (1) `firmware/` no declaraba `[workspace]` propio y quedaba atrapado por el workspace raíz del repo sin ser miembro — se agregó `[workspace]` vacío a `firmware/Cargo.toml`; (2) el build nativo de `esp-idf-sys` rechaza rutas de proyecto largas en Windows ("Shorten your project path down to no more than 10 characters") — se verificó copiando el proyecto a `C:\e\fw` (ruta corta, fuera del repo, descartada después). Con eso el build de ESP-IDF avanzó correctamente (toolchain, cfg flags de esp32, linker args, todo generado sin error) y **todo el grafo de dependencias Rust puras compiló sin errores de tipos ni sintaxis**. El build se frenó recién en el build script de `lr1121-modem-e`: `firmware/lr1121-modem-e/vendor/lr1121_modemE_driver/` y `firmware/lr1121-transceiver/vendor/lr11xx_driver/` están **vacíos** — el código C real de SWDR009/SWDR001 nunca se vendoreó en el repo, pese a que las tareas 2.1 y 4.1 de `migrate-lr1121-au915/tasks.md` están marcadas `[x]`. Esto bloquea cualquier build completo del firmware hoy, independientemente de este fix — no se intentó resolverlo (vendorear el SDK de Semtech no es parte del alcance de esta change). Sí se confirmó, hasta donde el build llegó, que no hay ningún error introducido por los cambios de frecuencia de esta change.
