## 1. Firmware compartido (weather-core)

- [x] 1.1 En `weather-core/src/udp_forwarder.rs`, cambiar la firma de `build_rxpk_json` de `(raw, rssi, snr, tmst_us)` a `(raw, freq_mhz, rssi, snr, tmst_us)` y reemplazar el literal `"freq":433.175` por `"freq":{freq_mhz}` en el `format!`. No requiere hardware físico para compilar. Commit sugerido: `fix(firmware): parametrizar freq en build_rxpk_json en vez de hardcodear 433.175 MHz`.

## 2. Firmware del gateway (call sites)

- [x] 2.1 En `firmware/src/bin/gateway-node.rs` (línea ~131), actualizar la llamada a `build_rxpk_json` para pasar `AU915_SUBBAND2_FREQ_HZ as f64 / 1e6` (ya está importado de `lr1121_transceiver`) como nuevo argumento `freq_mhz`. No requiere hardware físico para compilar. Commit sugerido: `fix(gateway): pasar frecuencia AU915 real a build_rxpk_json en gateway-node`.
- [x] 2.2 En `firmware/src/bin/gateway-node-mock.rs`, agregar `const CHANNEL_FREQ_MHZ: f64 = 916.8;` y pasarla en las dos llamadas a `build_rxpk_json` (uplink normal ~línea 177, join request en `join_via_udp` ~línea 240). Corregir además los logs de las líneas 51-52 ("433.175MHz (EU433)" → "916.8MHz (AU915 sub-band 2, canal 8)"). No requiere hardware físico para compilar. Commit sugerido: `fix(gateway): corregir freq y logs de gateway-node-mock a AU915 916.8 MHz`.

## 3. Firmware del nodo sensor mock (migración LR1121)

- [x] 3.1 Reescribir `firmware/src/bin/sensor-node-mock.rs` siguiendo el patrón de `firmware/src/bin/sensor-node.rs`: reemplazar el bloque `Sx1278::new` + `lorawan::otaa_join`/`lorawan::send_uplink` por `lr1121_modem_e::{ModemE, JOIN_TIMEOUT_MS}` — inicializar SPI igual que `sensor-node.rs` (SCK=18, MOSI=23, MISO=19, NSS=5) agregando los pines LR1121 que `sensor-node-mock.rs` no tiene hoy (BUSY=27, DIO1=26), llamar `modem.configure_au915_subband2(...)` y `modem.join(JOIN_TIMEOUT_MS)`, y sustituir `lorawan::send_uplink` por `modem.request_uplink(&payload)`. Mantener sin cambios: `device_id = 2`, `MockEnvironmentSensor`, el cálculo de `lluvia`/`viento` basado en `seq`, y `bateria_mv = 3700`. Actualizar el doc comment del archivo (líneas 1-15) y el log de arranque (línea 39: "LoRaWAN EU433 433.175 MHz" → "LoRaWAN AU915 sub-band 2 via Modem-E v2.1.0"). No requiere hardware físico para compilar. Commit sugerido: `feat(firmware): migrar sensor-node-mock de SX1278/EU433 a lr1121-modem-e/AU915`.

## 4. Limpieza de código legacy SX1278

- [x] 4.1 Tras 3.1, verificar por grep que `crate::radio::Sx1278` y las funciones `otaa_join`/`send_uplink` de `firmware/src/lorawan/mod.rs` ya no tienen ningún caller en `firmware/src/`. Si es así, eliminar `firmware/src/radio.rs` completo y las funciones `otaa_join`/`send_uplink` de `firmware/src/lorawan/mod.rs` (conservar los `pub use weather_core::lorawan::{crypto, frame, session}` — siguen en uso por `gateway-node-mock.rs`), y quitar `pub mod radio;` de `firmware/src/lib.rs`. No requiere hardware físico. Commit sugerido: `chore(firmware): eliminar driver SX1278 legacy sin uso tras migrar sensor-node-mock`.

## 5. Documentación y specs

- [x] 5.1 Corregir `openspec/changes/mock-sensor-firmware/design.md`: reemplazar las referencias a `migrate-lorawan-sx1278`, "SX1278" y el diagrama de flujo `LoRaWAN AU915 sub-band 2 SF7BW125` en vez de `EU433 433.175 MHz SF7BW125` (línea ~33), y la fila de la tabla "GPIO init" (línea ~91) para reflejar los pines LR1121 (BUSY, DIO1) en vez de "Solo SPI + reset (SX1278)". No requiere hardware físico. Commit sugerido: `docs(docs): corregir design.md de mock-sensor-firmware a LR1121/AU915`.
- [x] 5.2 Corregir `openspec/changes/mock-sensor-firmware/specs/mock-sensor-node/spec.md` (línea ~59): "ESP32 DevKitC V1 + módulo SX1278" → "ESP32 DevKitC V1 + módulo LR1121 (Modem-E)", y cualquier otra referencia a SX1278/EU433/433.175 en ese archivo. No requiere hardware físico. Commit sugerido: `docs(docs): corregir delta spec mock-sensor-node a LR1121/AU915`.
- [x] 5.3 Corregir `openspec/changes/mock-sensor-firmware/tasks.md` (línea ~24): referencia a "+ SX1278 + gateway corriendo" → "+ LR1121 + gateway corriendo". No requiere hardware físico. Commit sugerido: `docs(docs): corregir tasks.md de mock-sensor-firmware a LR1121`.
- [x] 5.4 Corregir `infra/SETUP.md`: renombrar el device profile `esp32-sensor-eu433` a `esp32-sensor-au915` en todas sus apariciones (secciones 3.1, 5.2, y cualquier otra), cambiar las referencias a "EU433"/"LoRaWAN EU433" por "AU915 sub-band 2", y actualizar la tabla de la sección 5 ("SX1278" → "LR1121", `sensor-node-mock` ya no requiere SX1278 tras la tarea 3.1). No requiere hardware físico. Commit sugerido: `docs(infra): corregir SETUP.md de EU433/SX1278 a AU915/LR1121`.

## 6. Verificación

- [x] 6.1 Correr una búsqueda de texto sobre `firmware/`, `weather-core/`, `openspec/changes/mock-sensor-firmware/`, `openspec/specs/gateway-node-mock/`, e `infra/SETUP.md` (patrón `433\.175|EU433|Sx1278|SX1278`) y confirmar cero resultados fuera de: comentarios históricos ya excluidos de alcance (`AGENTS.md`, `hardware/netlist.md`, `firmware/docs/hardware-assumptions.md`, `firmware/README.md`, `docs/hardware-questions.md`, `firmware/gateway-mock/`) y el `firmware/src/lorawan/mod.rs`/`radio.rs` si la tarea 4.1 decidió no eliminarlos. No requiere hardware físico. Commit sugerido: `chore(docs): verificar ausencia de referencias residuales a SX1278/EU433 en firmware mock`.
  - Resultado: `radio.rs` y las funciones SX1278 de `lorawan/mod.rs` se eliminaron (tarea 4.1), así que no aparecen en el grep. Los únicos matches restantes son: (a) comentarios cosméticos que describen correctamente que un binario *no* usa SX1278 (`firmware/Cargo.toml:28`, `gateway-node-mock.rs:1,12`, `gateway-node.rs:7` — "sin cambios respecto a la versión SX1278" es históricamente exacto), (b) los archivos explícitamente fuera de alcance (`firmware/docs/hardware-assumptions.md`, `firmware/README.md`, `firmware/gateway-mock/src/main.rs` — label cosmético `gateway-mock-eu433`), y (c) `openspec/specs/gateway-node-mock/spec.md` (spec shipped) que todavía muestra `freq=433.175` — esperado, la delta spec de este change (`specs/gateway-node-mock/spec.md`) se sincroniza a la spec principal recién al archivar el change (`openspec-sync-specs`), no antes.
- [x] 6.2 Verificar que `firmware/src/bin/gateway-node-mock.rs`, `firmware/src/bin/gateway-node.rs` y `firmware/src/bin/sensor-node-mock.rs` compilan tras los cambios (`cargo check --bin <nombre>` con el toolchain `esp`, o al menos confirmar que el grafo de dependencias Rust puras type-checka si el build completo sigue bloqueado por el SDK vendored ausente — ver nota en `fix-au915-subband2-frequency/tasks.md` tarea 6.2). No requiere hardware físico para compilar. Commit sugerido: `chore(firmware): verificar compilación de binarios mock tras migración AU915/LR1121`.
  - Resultado: `weather-core` (crate Rust puro, sin dependencias ESP-IDF — contiene el fix crítico de `udp_forwarder.rs`) se verificó con `cargo check` nativo (sin toolchain esp, sin ruta corta) y **compila limpio, sin errores** — confirma que el cambio de firma de `build_rxpk_json` (tarea 1.1) es correcto. Para los binarios ESP32 (`gateway-node-mock`, `gateway-node`, `sensor-node-mock`) se repitió el procedimiento de `fix-au915-subband2-frequency` (copia a `C:\e\fw` por el límite de ruta de ESP-IDF en Windows, toolchain `esp` ya instalado): `cargo check --bin gateway-node-mock` avanzó igual que antes (toolchain, cfg flags esp32, linker args generados sin error) pero se frenó de nuevo en el build script de un crate LR1121 por el mismo SDK vendored ausente (esta vez `lr1121-transceiver` — `liblr11xx.a` no existe; en la verificación anterior había sido `lr1121-modem-e`, es solo no-determinismo del orden de build paralelo, mismo gap subyacente). Esto significa que ni antes ni ahora `weather-firmware` (el crate que contiene los tres binarios editados) llegó a ser type-checkeado por rustc — ni siquiera `sensor-node.rs`, ya integrado desde `migrate-lr1121-au915`, pasó nunca por este chequeo, por el mismo bloqueo preexistente. La migración de `sensor-node-mock.rs` en la tarea 3.1 sigue exactamente el patrón de `sensor-node.rs` (mismo tipo de pines, misma secuencia `ModemE::new`→`configure_au915_subband2`→`join`→`request_uplink`), así que el riesgo de un error de tipos no detectado es bajo, pero queda pendiente una verificación real de compilación de `weather-firmware` cuando se resuelva el vendoring del SDK de Semtech (fuera de alcance de esta change, mismo hallazgo que en `fix-au915-subband2-frequency`). Directorios temporales de build (`C:\e\fw`, `C:\e\weather-core`) limpiados al finalizar.
